name: Distribute Infrastructure

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      target:
        description: 'Target repo name (or "all" for all repos)'
        default: 'all'
        type: string
      version:
        description: 'Version tag (auto-detected from release)'
        required: false
        type: string

permissions:
  contents: read

jobs:
  # ============================================================================
  # prepare: build the list of repos to process and resolve the version tag.
  # Output is consumed by the `distribute` matrix below.
  # ============================================================================
  prepare:
    name: Prepare repository list
    runs-on: ubuntu-latest
    # Distribution must run ONLY for stable releases. We deliberately use a
    # WHITELIST (positive check) rather than a blacklist: any new release flag
    # GitHub introduces in the future will be rejected by default until this
    # condition is explicitly extended. A stable GitHub release is defined as
    # the conjunction of two boolean flags on the `release` event payload:
    #   - prerelease == false   (the "Set as a pre-release" checkbox is off)
    #   - draft      == false   (the release is actually published, not draft)
    # Anything else — prerelease, draft, or any hypothetical future variant —
    # must not propagate the version across 26 target repos, because the
    # @diplodoc/infra version it advertises would not be on the npm `latest`
    # dist-tag and `npm ci` downstream would 404.
    #
    # Deprecate is a workflow_dispatch action in release.yml; it never produces
    # a `release` event and therefore cannot reach this branch.
    #
    # `workflow_dispatch` is a manual recovery channel — we trust the operator.
    if: |
      (github.event_name == 'release'
        && github.event.release.prerelease == false
        && github.event.release.draft == false) ||
      github.event_name == 'workflow_dispatch'
    outputs:
      repos: ${{ steps.repos.outputs.list }}
      version: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js and install dependencies
        uses: diplodoc-platform/setup-node-action@v1
        with:
          node-version: ${{ vars.NODE_VERSION || '24' }}
          cache: ''

      - name: Determine version
        id: version
        # workflow_dispatch inputs and event payloads are user-controlled.
        # Route them through env: and reference as "$VAR" to avoid
        # interpolating raw values into the shell body.
        env:
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          if [[ "$EVENT_NAME" == "release" ]]; then
            echo "tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"
          elif [[ -n "$INPUT_VERSION" ]]; then
            echo "tag=$INPUT_VERSION" >> "$GITHUB_OUTPUT"
          else
            VERSION=$(node -p "require('./package.json').version")
            echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"
          fi

      # The release event for the source repo fires in parallel with the publish
      # workflow that pushes the tarball to npmjs.org. If we start distribution
      # immediately we may rewrite `@diplodoc/infra` in 26 target repos to a
      # version that does not yet exist on the registry — `npm ci` in those
      # repos will then 404 and the freshly merged PRs will brick CI on master.
      # Wait for the version to actually appear on the public registry before
      # proceeding. Hard-fail after ~10 minutes; that is long enough to absorb
      # transient publish delays but short enough to surface real publish
      # failures (e.g. NPM_TOKEN permissions, registry outages).
      - name: Wait for @diplodoc/infra to be published on npm
        env:
          VERSION: ${{ steps.version.outputs.tag }}
        run: |
          PKG="@diplodoc/infra"
          # Strip leading "v" from tag (release tags are vX.Y.Z, npm versions are X.Y.Z).
          NPM_VERSION="${VERSION#v}"
          MAX_ATTEMPTS=60
          SLEEP_SECONDS=10
          echo "Waiting for ${PKG}@${NPM_VERSION} on npm registry…"
          for i in $(seq 1 ${MAX_ATTEMPTS}); do
            FOUND=$(npm view "${PKG}@${NPM_VERSION}" version 2>/dev/null || true)
            if [[ "${FOUND}" == "${NPM_VERSION}" ]]; then
              echo "::notice title=npm publish verified::${PKG}@${NPM_VERSION} is available after ${i} attempt(s)."
              exit 0
            fi
            echo "Attempt ${i}/${MAX_ATTEMPTS}: ${PKG}@${NPM_VERSION} not on npm yet, sleeping ${SLEEP_SECONDS}s…"
            sleep ${SLEEP_SECONDS}
          done
          echo "::error title=npm publish missing::${PKG}@${NPM_VERSION} was not published within $((MAX_ATTEMPTS * SLEEP_SECONDS / 60)) minutes. Aborting distribution to avoid breaking downstream repos."
          exit 1

      - name: Build repo list
        id: repos
        # Funnel the user-controlled `target` input through env. Also build the
        # JSON list with JSON.stringify in node (not by string-interpolating
        # $TARGET into the YAML/JSON output) so quotes / backslashes in the
        # input are JSON-escaped rather than corrupting the array literal.
        env:
          INPUT_TARGET: ${{ inputs.target }}
        run: |
          export TARGET="${INPUT_TARGET:-all}"

          if [[ "$TARGET" != "all" ]]; then
            SAFE_LIST=$(node -e 'process.stdout.write(JSON.stringify([process.env.TARGET]))')
            echo "list=${SAFE_LIST}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Extract repo names from distribution.yml (only from repos: section)
          REPOS=$(node -e "
            const yaml = require('js-yaml');
            const fs = require('fs');
            const config = yaml.load(fs.readFileSync('distribution.yml', 'utf8'));
            const repos = Object.keys(config.repos || {});
            console.log(JSON.stringify(repos));
          ")

          echo "list=${REPOS}" >> "$GITHUB_OUTPUT"

  # ============================================================================
  # distribute: a single matrix that does ALL the work per repo —
  # clone target, apply scaffolding, detect diff, push branch, open PR, enable
  # auto-merge. Each matrix cell ends green (✓) on either "updated" or "skipped"
  # (skip is a valid outcome). A real failure turns the cell red (✗).
  #
  # Each cell writes a tiny `status-<repo>.json` artifact with the per-repo
  # outcome; the downstream `report` job aggregates them into a markdown summary
  # table — that table is where the grey-icon "skipped" signal lives now
  # (GitHub Actions does not expose a per-cell neutral state since 2020).
  # ============================================================================
  distribute:
    name: 'Update ${{ matrix.repo }}'
    needs: prepare
    if: needs.prepare.outputs.repos != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repo: ${{ fromJson(needs.prepare.outputs.repos) }}
      fail-fast: false
      max-parallel: 5
    # matrix.repo is derived from a user-controlled input (workflow_dispatch
    # inputs.target) or from distribution.yml. Either way, we must never let
    # the raw value reach a shell line as ${{ matrix.repo }} — that would
    # be a classic injection vector (quotes / $(...) / ;-chains). Funnel it
    # through env vars and reference as "$REPO_NAME" / "$REPO" instead.
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
      REPO_NAME: ${{ matrix.repo }}
      REPO: 'diplodoc-platform/${{ matrix.repo }}'
    steps:
      - name: Record start time
        id: start
        run: echo "ts=$(date -u +%s)" >> "$GITHUB_OUTPUT"

      - name: Checkout infra repo
        uses: actions/checkout@v4

      - name: Generate App token for target repo
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.INFRA_APP_ID }}
          private-key: ${{ secrets.INFRA_APP_PRIVATE_KEY }}
          owner: diplodoc-platform
          # Action `with:` inputs are not shell — they are passed verbatim to
          # the action runtime — so direct ${{ matrix.repo }} here is fine.
          repositories: ${{ matrix.repo }}

      - name: Setup Node.js and install dependencies
        uses: diplodoc-platform/setup-node-action@v1
        with:
          node-version: ${{ vars.NODE_VERSION || '24' }}
          cache: ''

      - name: Clone target repository
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          gh repo clone "diplodoc-platform/${REPO_NAME}" target -- --depth 1
          cd target
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/diplodoc-platform/${REPO_NAME}.git"

      - name: Apply scaffolding
        run: |
          node bin/infra.js sync \
            --target ./target \
            --repo "${REPO_NAME}" \
            --config ./distribution.yml

      - name: Update @diplodoc/infra version in package.json
        working-directory: target
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            const version = '${VERSION}'.replace(/^v/, '');
            let updated = false;

            for (const depType of ['devDependencies', 'dependencies']) {
              if (pkg[depType] && pkg[depType]['@diplodoc/infra']) {
                pkg[depType]['@diplodoc/infra'] = version;
                updated = true;
              }
            }

            if (updated) {
              fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
              console.log('Updated @diplodoc/infra to ' + version);
            } else {
              console.log('@diplodoc/infra not found in dependencies, skipping');
            }
          "

      - name: Check for changes
        id: changes
        working-directory: target
        run: |
          if [[ -z $(git status --porcelain) ]]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            echo "::notice title=No changes::No scaffolding changes for ${REPO_NAME} — nothing to distribute."
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            echo "Changes in ${REPO_NAME}:"
            git diff --stat
          fi

      - name: Resolve auto-merge flag
        id: auto-merge
        if: steps.changes.outputs.has_changes == 'true'
        # Do NOT splice ${{ matrix.repo }} into the node -e source string —
        # quotes / backslashes in the repo name would close the JS string
        # literal and execute arbitrary JS (full RCE in the runner). Read
        # the repo name from process.env instead.
        run: |
          AUTO_MERGE=$(node -e "
            const yaml = require('js-yaml');
            const fs = require('fs');
            const config = yaml.load(fs.readFileSync('distribution.yml', 'utf8'));
            const defaults = config.defaults || {};
            const repo = (config.repos || {})[process.env.REPO_NAME] || {};
            const autoMerge = repo.auto_merge !== undefined ? repo.auto_merge : (defaults.auto_merge || false);
            console.log(autoMerge ? 'true' : 'false');
          ")
          echo "value=${AUTO_MERGE}" >> "$GITHUB_OUTPUT"

      - name: Create pull request
        id: pr
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: target
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          BRANCH="infra/update-${VERSION}"

          # ----- Pre-flight: detect directory-file conflict in remote refs -----
          # Git stores refs as a filesystem-like tree. If a leaf ref `refs/heads/<parent>`
          # exists (e.g. `infra`), we cannot create `refs/heads/<parent>/<child>`
          # (e.g. `infra/update-v1.0.0`) — the path is occupied by a "file".
          # Fail early with a clear message instead of getting a cryptic
          # "remote rejected ... (directory file conflict)" from git push.
          #
          # NOTE: do NOT use `gh api .../git/refs/heads/<ref>` — that endpoint returns
          # 200 for both exact match AND prefix match (e.g. it returns `refs/heads/infra/*`
          # when asked about `refs/heads/infra`). We need an EXACT match only.
          # `git ls-remote` returns all matching refs as a list; we filter for the
          # exact ref name with `grep -Fxq`.
          PARENT_REF="${BRANCH%%/*}"
          if [[ "$PARENT_REF" != "$BRANCH" ]]; then
            if git ls-remote origin "refs/heads/${PARENT_REF}" 2>/dev/null \
                | awk '{print $2}' \
                | grep -Fxq "refs/heads/${PARENT_REF}"; then
              echo "::error::Repository ${REPO} has a conflicting ref 'refs/heads/${PARENT_REF}'."
              echo "::error::This blocks creation of '${BRANCH}' (git directory/file conflict)."
              echo "::error::Resolve manually, then re-run this workflow for target=${REPO_NAME}:"
              echo "::error::  gh api -X DELETE repos/${REPO}/git/refs/heads/${PARENT_REF}"
              exit 1
            fi
          fi

          # ----- Commit & push the branch -----
          git checkout -B "${BRANCH}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A
          git commit -m "chore: update infrastructure to ${VERSION}"
          git push -u --force origin "${BRANCH}"

          # ----- Create PR if it doesn't exist -----
          # Note: all gh commands below use explicit --repo to avoid ambiguity in forked
          # repos (gh CLI defaults to upstream when the target repository is a fork).
          EXISTING_PR=$(gh pr list --repo "${REPO}" --head "${BRANCH}" --json number --jq '.[0].number' 2>/dev/null || echo "")
          if [[ -n "$EXISTING_PR" ]]; then
            echo "PR already exists: #${EXISTING_PR} — skipping creation."
            PR_URL="https://github.com/${REPO}/pull/${EXISTING_PR}"
            echo "pr_url=${PR_URL}" >> "$GITHUB_OUTPUT"
            echo "pr_number=${EXISTING_PR}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          DEFAULT_BRANCH=$(gh repo view "${REPO}" --json defaultBranchRef --jq '.defaultBranchRef.name')

          PR_URL=$(gh pr create \
            --repo "${REPO}" \
            --head "${BRANCH}" \
            --base "${DEFAULT_BRANCH}" \
            --title "chore: update infrastructure to ${VERSION}" \
            --body "$(cat <<BODY
          ## Automated Infrastructure Update

          This PR was automatically created by the infrastructure distribution workflow.

          **Source:** [@diplodoc/infra](https://github.com/diplodoc-platform/infra)
          **Version:** [\`${VERSION}\`](https://github.com/diplodoc-platform/infra/releases/tag/${VERSION})

          ### What changed
          Infrastructure scaffolding files have been updated to the latest version.
          This includes linting configs, CI workflows, and other shared configuration.

          See the [release notes](https://github.com/diplodoc-platform/infra/releases/tag/${VERSION}) for the full changelog.

          ---
          _If this PR breaks CI, please review the changes and adjust as needed._
          _To exclude specific files from future updates, add them to \`.infrarc.yml\` (see [docs](https://github.com/diplodoc-platform/infra#blacklist))._
          BODY
          )")

          echo "Created PR: ${PR_URL}"
          PR_NUMBER=$(echo "${PR_URL}" | awk -F/ '{print $NF}')
          echo "pr_url=${PR_URL}" >> "$GITHUB_OUTPUT"
          echo "pr_number=${PR_NUMBER}" >> "$GITHUB_OUTPUT"

          # ----- Attach label (best-effort, non-fatal) -----
          # In some repos (notably forks with disabled Issues) labels API may be
          # unavailable. We still want the PR to exist even if labelling fails.
          if gh label create "infra-auto" \
              --repo "${REPO}" \
              --color "0E8A16" \
              --description "Automated infrastructure update PR" 2>&1 | grep -v "already exists" || true; then
            :
          fi

          if ! gh pr edit "${PR_URL}" --add-label "infra-auto"; then
            echo "::warning::Could not add 'infra-auto' label to ${PR_URL}. PR was created without it."
            echo "::warning::This usually means Issues are disabled in ${REPO} (common for forks)."
          fi

      # GitHub auto-merge does NOT use Ruleset `bypass_actors` to satisfy the
      # `pull_request` rule (e.g. required_approving_review_count: 1 or
      # require_code_owner_review: true). A bypass only grants the right to
      # merge DIRECTLY past the queue; the auto-merge queue itself still waits
      # for actual approving reviews from non-bypassed actors.
      #
      # On top of that, **GitHub Apps cannot be code-owners** — CODEOWNERS
      # syntax accepts only @user and @org/team handles. Since every repo
      # gets `* @diplodoc-platform/team` from the infra scaffolding, an App
      # review cannot satisfy require_code_owner_review.
      #
      # Solution: use a real machine user (`diplodoc-bot`) that is a member
      # of @diplodoc-platform/team. Its PR review counts as a code-owner
      # approval. The token below is a fine-grained PAT scoped to PR-write
      # on the 26 target repos; it MUST belong to a user that is also NOT
      # the PR author (the PR is opened by INFRA_APP_ID; GitHub forbids
      # self-approval).
      #
      # Skip when auto_merge is off for the repo: a human will decide whether
      # to approve and merge; the bot must not silently approve PRs that a
      # maintainer explicitly chose to gate manually.
      - name: Auto-approve PR via diplodoc-bot machine user
        id: approve
        if: steps.changes.outputs.has_changes == 'true' && steps.auto-merge.outputs.value == 'true'
        env:
          # Fine-grained PAT issued by `diplodoc-bot` user account. Required
          # scopes: `Pull requests: Read and write` on the 26 distribution
          # target repos. NOT an App token — because Apps cannot be CODEOWNERS.
          GH_TOKEN: ${{ secrets.INFRA_APPROVER_PAT }}
          PR_NUMBER: ${{ steps.pr.outputs.pr_number }}
        run: |
          if [[ -z "${PR_NUMBER}" ]]; then
            echo "::warning::No PR number recorded — skipping auto-approve."
            echo "approved=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [[ -z "${GH_TOKEN}" ]]; then
            echo "::warning::INFRA_APPROVER_PAT secret is not set — skipping auto-approve."
            echo "approved=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if gh pr review "${PR_NUMBER}" \
                --repo "${REPO}" \
                --approve \
                --body "Auto-approved by diplodoc-bot (machine user, member of @diplodoc-platform/team) to satisfy required_approving_review_count and require_code_owner_review for the mechanical infrastructure update. Human review of the change itself happens upstream in diplodoc-platform/infra."; then
            echo "::notice title=Approved::${REPO}#${PR_NUMBER}"
            echo "approved=true" >> "$GITHUB_OUTPUT"
          else
            echo "::warning::Failed to auto-approve ${REPO}#${PR_NUMBER}. PR remains awaiting review."
            echo "approved=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Enable auto-merge
        id: automerge
        if: steps.changes.outputs.has_changes == 'true' && steps.auto-merge.outputs.value == 'true'
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          PR_NUMBER: ${{ steps.pr.outputs.pr_number }}
        run: |
          if [[ -z "$PR_NUMBER" ]]; then
            echo "::warning::No PR number recorded, skipping auto-merge"
            exit 0
          fi

          if gh pr merge "$PR_NUMBER" --repo "${REPO}" --auto --rebase; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "::warning::Auto-merge not available for ${REPO} (likely missing branch protection rules)"
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      # Always write a tiny status-<repo>.json artifact, even on failure. This is
      # the only channel the `report` job reads to build the summary table.
      # Schema:
      #   {
      #     "repo": "<name>",
      #     "status": "updated" | "skipped" | "failed",
      #     "pr_url"?: string,
      #     "pr_number"?: string,
      #     "auto_merge"?: "true" | "false",
      #     "auto_merge_enabled"?: "true" | "false",
      #     "duration_s": number,
      #     "started_at": "<ISO-8601>"
      #   }
      - name: Write status artifact payload
        id: status
        if: always()
        # All values consumed by the node script below come from process.env —
        # we deliberately avoid ${{ ... }} interpolation INSIDE the node -e
        # source string. matrix.repo / status / duration / started_at could
        # otherwise contain quotes or backslashes that would close the JS
        # string literal and execute attacker-controlled code.
        env:
          STARTED_TS: ${{ steps.start.outputs.ts }}
          HAS_CHANGES: ${{ steps.changes.outputs.has_changes }}
          AUTO_MERGE: ${{ steps.auto-merge.outputs.value }}
          AUTO_MERGE_ENABLED: ${{ steps.automerge.outputs.enabled }}
          PR_URL: ${{ steps.pr.outputs.pr_url }}
          PR_NUMBER: ${{ steps.pr.outputs.pr_number }}
          JOB_STATUS: ${{ job.status }}
        run: |
          mkdir -p .status
          NOW_TS=$(date -u +%s)
          STARTED_TS_SAFE="${STARTED_TS:-$NOW_TS}"
          DURATION=$(( NOW_TS - STARTED_TS_SAFE ))
          STARTED_AT=$(date -u -d "@${STARTED_TS_SAFE}" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -r "${STARTED_TS_SAFE}" +%Y-%m-%dT%H:%M:%SZ)

          # Pick status based on job state and detected changes.
          # `job.status` is 'success' | 'failure' | 'cancelled' at this point.
          if [[ "${JOB_STATUS}" != "success" ]]; then
            STATUS="failed"
          elif [[ "${HAS_CHANGES}" == "true" ]]; then
            STATUS="updated"
          else
            STATUS="skipped"
          fi

          export STATUS DURATION STARTED_AT
          # node -e source uses SINGLE quotes (no shell expansion) and reads
          # every potentially-tainted value from process.env. Do NOT switch
          # back to double-quoting plus workflow-expression interpolation
          # of inputs/matrix — that would re-introduce the JS-literal
          # injection that this rewrite removes.
          node -e '
            const fs = require("fs");
            const payload = {
              repo: process.env.REPO_NAME,
              status: process.env.STATUS,
              duration_s: Number(process.env.DURATION) || 0,
              started_at: process.env.STARTED_AT,
            };
            if (process.env.PR_URL) payload.pr_url = process.env.PR_URL;
            if (process.env.PR_NUMBER) payload.pr_number = process.env.PR_NUMBER;
            if (process.env.AUTO_MERGE) payload.auto_merge = process.env.AUTO_MERGE;
            if (process.env.AUTO_MERGE_ENABLED) payload.auto_merge_enabled = process.env.AUTO_MERGE_ENABLED;
            fs.writeFileSync(".status/status-" + process.env.REPO_NAME + ".json", JSON.stringify(payload, null, 2));
            console.log(JSON.stringify(payload));
          '

      - name: Upload status artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: status-${{ matrix.repo }}
          path: .status/status-${{ matrix.repo }}.json
          retention-days: 1
          if-no-files-found: error

  # ============================================================================
  # report: aggregate per-repo status artifacts into a single markdown summary
  # table written to $GITHUB_STEP_SUMMARY. Runs even if some distribute cells
  # failed (`if: always()`). Marks the run as failed (`core.setFailed`) only
  # when at least one repo ended in `failed` state.
  #
  # This table is where the "Skipped" signal lives now — per-cell grey icons
  # are not achievable in GitHub Actions (no neutral exit since 2020), so the
  # rich summary replaces them with explicit ⊘ rows including PR links,
  # durations and reasons.
  # ============================================================================
  report:
    name: Build distribution report
    needs: [prepare, distribute]
    if: always() && needs.prepare.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Download all status artifacts
        uses: actions/download-artifact@v4
        with:
          path: .status
          pattern: status-*
          merge-multiple: true

      # Auto-merge does not actually merge synchronously: GitHub waits for CI
      # to go green (or fail) and only then resolves the merge. We poll each
      # PR for up to ~2 minutes to enrich the summary table with the actual
      # post-distribution outcome (merged / blocked / dirty / still pending).
      # Without this poll the table would show "auto-merge requested" for
      # every successful run and hide real failures behind a green tick.
      - name: Generate App token for PR status polling
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.INFRA_APP_ID }}
          private-key: ${{ secrets.INFRA_APP_PRIVATE_KEY }}
          owner: diplodoc-platform
      - name: Poll merge state for distributed PRs
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          set -euo pipefail
          shopt -s nullglob
          # Collect (repo, pr_number, status-file) triples we need to follow.
          # Using jq (preinstalled on GitHub-hosted runners) keeps this robust
          # against status payloads with quotes/backslashes in string fields.
          declare -a TASKS=()
          for f in .status/status-*.json; do
            REPO=$(jq -r '.repo // ""' "$f")
            PR=$(jq -r '.pr_number // ""' "$f")
            if [[ -n "${REPO}" && -n "${PR}" ]]; then
              TASKS+=("${REPO}|${PR}|${f}")
            fi
          done
          if [[ ${#TASKS[@]} -eq 0 ]]; then
            echo "::notice title=Polling skipped::No PRs to poll."
            exit 0
          fi

          # Poll up to 24 rounds × 5s = 2 minutes.
          # We exit early as soon as every PR is in a terminal state.
          MAX_ROUNDS=24
          SLEEP_SECONDS=5
          for round in $(seq 1 ${MAX_ROUNDS}); do
            ALL_TERMINAL=true
            for task in "${TASKS[@]}"; do
              IFS='|' read -r repo pr file <<<"${task}"
              json=$(gh pr view "${pr}" --repo "diplodoc-platform/${repo}" \
                --json state,mergeStateStatus,mergedAt,mergeable 2>/dev/null || echo '{}')
              state=$(jq -r '.state // ""' <<<"${json}")
              merge_state=$(jq -r '.mergeStateStatus // ""' <<<"${json}")
              merged_at=$(jq -r '.mergedAt // ""' <<<"${json}")
              # Update the status artifact in place so the renderer below picks
              # up the freshest values. Pass the new fields as jq args (--arg)
              # rather than splicing them into a script string, which would
              # mis-handle empty values and any future special characters.
              tmp=$(mktemp)
              jq \
                --arg state "${state}" \
                --arg ms "${merge_state}" \
                --arg merged_at "${merged_at}" \
                '.pr_state = $state
                 | .pr_merge_state = $ms
                 | (if ($merged_at | length) > 0 then .pr_merged_at = $merged_at else . end)' \
                "${file}" > "${tmp}"
              mv "${tmp}" "${file}"
              # Terminal states: MERGED / CLOSED / explicitly blocked / dirty.
              # CLEAN/UNSTABLE/HAS_HOOKS/BEHIND/UNKNOWN — still resolving, keep polling.
              if [[ "${state}" != "MERGED" && "${state}" != "CLOSED" \
                    && "${merge_state}" != "BLOCKED" && "${merge_state}" != "DIRTY" ]]; then
                ALL_TERMINAL=false
              fi
            done
            if ${ALL_TERMINAL}; then
              echo "::notice title=Polling complete::All PRs reached a terminal state after ${round} round(s)."
              break
            fi
            sleep ${SLEEP_SECONDS}
          done

      - name: Render summary table
        uses: actions/github-script@v7
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          REPOS_JSON: ${{ needs.prepare.outputs.repos }}
        with:
          script: |
            const fs = require('fs');
            const path = require('path');

            const version = process.env.VERSION || '';
            const allRepos = JSON.parse(process.env.REPOS_JSON || '[]');

            // Read every status-<repo>.json file downloaded into .status/
            const statusDir = '.status';
            const statuses = new Map(); // repo -> payload
            if (fs.existsSync(statusDir)) {
              for (const name of fs.readdirSync(statusDir)) {
                if (!name.startsWith('status-') || !name.endsWith('.json')) continue;
                try {
                  const raw = fs.readFileSync(path.join(statusDir, name), 'utf8');
                  const data = JSON.parse(raw);
                  if (data && data.repo) statuses.set(data.repo, data);
                } catch (err) {
                  core.warning(`Failed to parse ${name}: ${err.message}`);
                }
              }
            }

            // Repos that ended without any status artifact at all are treated
            // as failed (typically: matrix cell crashed before reaching the
            // "Write status artifact payload" step, or upload failed).
            const rows = allRepos.map((repo) => {
              const s = statuses.get(repo);
              if (!s) {
                return {
                  repo,
                  status: 'failed',
                  reason: 'no status artifact produced',
                };
              }
              return s;
            });

            const fmtDuration = (s) => {
              if (typeof s !== 'number' || !Number.isFinite(s) || s < 0) return '—';
              const m = Math.floor(s / 60);
              const sec = s % 60;
              return m > 0 ? `${m}m ${sec}s` : `${sec}s`;
            };

            const icons = {
              updated: '✅ Updated',
              skipped: '⊘ Skipped',
              failed: '❌ Failed',
            };

            const counts = { updated: 0, skipped: 0, failed: 0 };
            for (const r of rows) counts[r.status] = (counts[r.status] || 0) + 1;

            // Per-repo annotations for quick UI scanning.
            for (const r of rows) {
              const head = `[${r.repo}]`;
              if (r.status === 'updated') {
                const pr = r.pr_url ? ` — ${r.pr_url}` : '';
                const am = r.auto_merge === 'true'
                  ? (r.auto_merge_enabled === 'true' ? ' (auto-merge enabled)' : ' (auto-merge requested but not enabled)')
                  : '';
                core.notice(`${head} ✅ Updated${pr}${am}`);
              } else if (r.status === 'skipped') {
                core.notice(`${head} ⊘ Skipped — no scaffolding changes`);
              } else {
                const reason = r.reason ? ` — ${r.reason}` : '';
                core.error(`${head} ❌ Failed${reason}`);
              }
            }

            // Sort: failed first, then updated, then skipped — alpha within group.
            const order = { failed: 0, updated: 1, skipped: 2 };
            rows.sort((a, b) => {
              const oa = order[a.status] ?? 99;
              const ob = order[b.status] ?? 99;
              if (oa !== ob) return oa - ob;
              return a.repo.localeCompare(b.repo);
            });

            const fmtMerge = (r) => {
              // Reflect the actual post-poll merge state of the PR, not just
              // whether auto-merge was requested. See "Poll merge state for
              // distributed PRs" step above for the source of these fields.
              const s = r.pr_state || '';
              const ms = r.pr_merge_state || '';
              if (s === 'MERGED') return '✅ merged';
              if (s === 'CLOSED') return '🚫 closed';
              if (ms === 'BLOCKED') return '⛔ blocked';
              if (ms === 'DIRTY') return '⚠️ dirty';
              if (ms === 'UNSTABLE') return '🟡 unstable';
              if (ms === 'BEHIND') return '⏳ behind';
              if (ms === 'CLEAN' || ms === 'HAS_HOOKS') return '⏳ pending';
              if (s === 'OPEN') return '⏳ open';
              return '—';
            };

            const formatRow = (r) => {
              const status = icons[r.status] || r.status;
              const pr = r.pr_url
                ? `[#${r.pr_number || '?'}](${r.pr_url})`
                : '—';
              let am = '—';
              if (r.auto_merge === 'true') {
                am = r.auto_merge_enabled === 'true' ? '✅ enabled' : '⚠️ requested';
              } else if (r.auto_merge === 'false') {
                am = 'off';
              }
              const merge = fmtMerge(r);
              const dur = fmtDuration(r.duration_s);
              return `| \`${r.repo}\` | ${status} | ${pr} | ${am} | ${merge} | ${dur} |`;
            };

            const title = version
              ? `Distribution summary — ${version}`
              : 'Distribution summary';

            const lines = [
              `## ${title}`,
              '',
              `Total: **${rows.length}** · ✅ Updated: **${counts.updated || 0}** · ⊘ Skipped: **${counts.skipped || 0}** · ❌ Failed: **${counts.failed || 0}**`,
              '',
              '| Repo | Status | PR | Auto-merge | Merge state | Duration |',
              '| --- | --- | --- | --- | --- | --- |',
              ...rows.map(formatRow),
            ];

            await core.summary.addRaw(lines.join('\n')).write();

            if ((counts.failed || 0) > 0) {
              core.setFailed(`${counts.failed} repo(s) failed to distribute. See the summary table above.`);
            }
